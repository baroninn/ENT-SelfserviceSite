@model SystemHostingPortal.Models.Office365Model
@{
    ViewBag.Title = "Office365 > Add Domain";
}
@section script {
    <script>
        $(document).ready(function () {
            $("select[name=organization]").change(function () {
                $("#acceptedDomain").html("Loading...");
                GetAcceptedDomain();
            });

            $("#acceptedDomain").html("Loading...");
            GetAcceptedDomain();
        });

        /*
        function GetExistingAcceptedDomain() {
            var organization = $("select[name=organization]").val();

            $("#existingAcceptedDomain").html('loading...');

            $.get("/Office365/GetAcceptedDomain?&organization=" + organization, function (data) {
                $("#existingAcceptedDomain").html(data);
            });
        }


        function GetExistingAcceptedDomains() {
            $("select[name=domain]").html('<option>loading...</option>');

            var organization = $("select[name=organization]").val();
            var selectedvalue = 'Model.AddDomain.Domain';

/*            $.get("/Office365/GetAcceptedDomain?selectedvalue=" + selectedvalue + "&organization=" + organization, function (data) {
                $("select[name=domain]").html(data);
            });
        }
        */
    </script>
}
<h1 class="title">@ViewBag.Title</h1>
<p>Use this form to add a UPN domain to the customers registered Office 365 tenant.</p>
@using (Html.BeginForm("AddDomain", "Office365"))
{
    <fieldset>
        <legend>Add UPN</legend>
        <table>
            <tr>
                <td colspan="2">
                    <div id="messageDiv">
                        @if (Model.Message != null)
                        {
                            <div class="message">@Model.Message <p><small><a href="javascript:ClearErrors()">Clear errors</a></small></p></div>
                        }
                    </div>
                    <div id="okMessageDiv">
                        @if (Model.OKMessage.Count > 0)
                        {
                            <div class="okmessage">
                                @foreach (string msg in Model.OKMessage)
                                {
                                    <p>@msg</p>
                                }
                                <p><small><a href="javascript:ClearMessages()">Clear messages</a></small></p>
                            </div>
                        }
                    </div>
                </td>
            </tr>
            <tr>
                <td class="lefttd">Organization</td>
                <td>
                    <select name="organization">
                        @foreach (string organization in Model.Organizations)
                        {
                            <option @if (Model.ActionFailed) { if (organization == Model.AddDomain.Organization) { @: selected="selected"
                                                                        } }>
                                @organization
                                                                    </option>
                        }
                    </select>
                </td>
            </tr>
            <tr>
                <td class="lefttd">Accepted emaildomains:</td>
                <td class="righttd">
                    <div id="acceptedDomain"></div>
                </td>
            </tr>
            <tr>
                <td class="lefttd tooltip"><span>Domain<i class="material-icons infoicon">info</i></span></td>
                <td>
                    <input name="domain" placeholder="testdomæne.dk" @if (Model.ActionFailed) { @: value="@Model.AddDomain.Domain"
                                                                                                             } />
                    <div class="description">
                        <p class="descriptiontext">
                            This will add a domain to the registered tenant in Office 365, and a UPN on the local on-prem AD.
                        </p>
                    </div>
                </td>
            </tr>

        </table>
        <p class="title">
            Stuff to do manually:
        </p>
        <p>Remember to verify the domain with DNS records in the O365 portal.</p>
        <br />
        <input type="submit" id="buttonSubmit" value="Add Domain" onclick="buttonSubmit.disabled = true; ClearMessages(); ClearErrors(); StartSubmitButtonMessage(); this.form.submit(); " />
    </fieldset>

}