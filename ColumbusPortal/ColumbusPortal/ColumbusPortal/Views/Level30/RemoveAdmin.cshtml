@model ColumbusPortal.Models.Level30Model
@{
    ViewBag.Title = "Level30 > Remove Admin";
}
@section script {

    <script>
        $(document).ready(function () {

            GetAdminUsersLIST();

            $("select[name=samaccountname]").html('<option>loading..</option>');
        });

    </script>
}

<h1 class="title">@ViewBag.Title</h1>
<p>Use this form to remove an Admin user..</p>
<br />

@using (Html.BeginForm("RemoveAdmin", "Level30"))
{
    @Html.AntiForgeryToken()
    <fieldset>
        <legend>Remove Admin</legend>
        <table>
            <tr>
                <td colspan="2">
                    <div id="messageDiv">
                        @if (Model.Message != null)
                        {
                            <div class="message">@Model.Message <p><small><a href="javascript:ClearErrors()">Clear errors</a></small></p></div>
                        }
                    </div>
                    <div id="okMessageDiv">
                        @if (Model.OKMessage.Count > 0)
                        {
                            <div class="okmessage">
                                @foreach (string msg in Model.OKMessage)
                                {
                                    <p>@msg</p>
                                }
                                <p><small><a href="javascript:ClearMessages()">Clear messages</a></small></p>
                            </div>
                        }
                    </div>
                </td>
            </tr>
        </table>
        <table>
            <tr>
                <td class="lefttd"><span>Admin Users<i class="material-icons infoicon"></i></span></td>
                <td class="middletd">
                    <select name="samaccountname"><option>loading...</option></select>
                    <span class="mandatory">*</span>
                </td>
            </tr>
        </table>

        <br />
        <input type="submit" class="button" id="buttonSubmit" value="Remove Admin" onclick="buttonSubmit.disabled = true; ClearMessages(); ClearErrors(); StartSubmitButtonMessage(); this.form.submit()" />
    </fieldset>

}
